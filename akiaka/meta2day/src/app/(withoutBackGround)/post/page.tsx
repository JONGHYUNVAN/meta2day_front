import PostForm from "@/app/(withoutBackGround)/post/pageForm";

export default function PostPage() {
    return (
        <div className="min-h-screen mt-1 flex items-center justify-center bg-transparent"
        >
                <PostForm/>
        </div>
    );
}
