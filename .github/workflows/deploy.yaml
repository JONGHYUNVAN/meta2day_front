name: CI/CD for Vercel-Next

on:
  push:
    branches: [master]

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ruby
          sudo gem install mustache

      - name: Move to the correct directory
        run: |
          cd akiaka/meta2day

      - name: Create output directory and copy files
        run: |
          mkdir -p output
          cp -R ./* ./output
          chmod +x ./build.sh
          sh ./build.sh

      - name: Check output directory
        run: |
          if [ -d "./output" ]; then
            echo "Output directory exists."
            ls -la ./output
          else
            echo "Output directory does not exist."
          fi

      - name: Push to personal repository
        id: push_directory
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.MYREPOSITORYKEY }}
        with:
          source-directory: 'output'
          destination-github-username: JONGHYUNVAN
          destination-repository-name: meta2day_front
          user-email: ${{ secrets.MYEMAIL }}
          commit-message: ${{ github.event.commits[0].message }}
          target-branch: main
